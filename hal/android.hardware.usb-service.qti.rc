# Copyright (c) 2023 Qualcomm Innovation Center, Inc. All rights reserved.
# SPDX-License-Identifier: BSD-3-Clause-Clear

on init
    chown system usb /sys/class/usb_notify/usb_control/disable
    chown system usb /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/usb_data_enabled

service vendor.usb-hal /vendor/bin/hw/android.hardware.usb-service.qti
    class hal
    user system
    group system mtp usb

on early-init
    # Needed to allow toggling USB without being root.
    chown root system /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/usb_data_enabled
    chown root system /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/mode
    chmod 664 /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/usb_data_enabled
    chmod 664 /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/mode
    # Disable USB data initially, even with existing connected device.
    write /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/usb_data_enabled 0

service enable_usb_data /vendor/bin/init.enable_usb_data.sh /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/usb_data_enabled 1 /sys/class/typec/port0-partner /sys/class/typec/port0/data_role [host] /sys/devices/platform/soc/${vendor.usb.device:-a600000.ssusb}/mode host peripheral
    disabled
    user nobody
    group system
    oneshot
